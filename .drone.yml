kind: pipeline
type: docker
name: default

platform:
  os: linux
  arch: arm64

steps:
- name: test
  image: rustlang/rust:nightly
  commands:
  - cargo build
  - cargo test
  - rustup component add clippy && cargo clippy -- -Dwarnings
  - cargo install cargo-audit && cargo audit

